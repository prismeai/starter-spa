slug: v1/status
name: API/v1/Status
description: 'Sync: HTTP endpoint that returns the current server status'
when:
  endpoint: v1/status
do:
  - set:
      name: result
      value:
        status: ok
        version: v2
        message: hello from the updated automation
        timestamp: '{{run.date}}'
output: '{{result}}'
